apiVersion: v1
kind: Pod
metadata:
  name: node-selector-taint
  labels:
    purpose: taints-tolerations-demo
spec:
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: Always
  nodeSelector:
    disktype: ssd